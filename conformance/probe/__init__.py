"""Raw wire probes for the Mistral scheduler (whoami, REST, SDK history
capture) plus the captured reference responses (*.json). Packaged as
mwf_conformance.probe; also runnable as plain scripts from a repo checkout.
Needs MISTRAL_API_KEY (env or .env) for anything live."""
