"""Live end-to-end drivers: drive a whole workflow through a running worker
against a local Temporal dev server or the live Mistral frontend. Packaged as
mwf_conformance.e2e; also runnable as plain scripts from a repo checkout (the
run_*.sh wrappers there assume repo paths)."""
