"""The recorded conformance vectors (package data): payloads/*.json — Mistral
payload envelopes (codec oracle), histories/*.json — Temporal event histories
(replay-engine oracle), plus README.md / NOTES.md documenting capture
provenance and the byte-level findings. Access via
mwf_conformance.load_golden() / list_goldens()."""
