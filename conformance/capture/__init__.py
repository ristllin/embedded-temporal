"""Capture tooling: (re)generates the golden vectors from the real pinned
SDKs (temporalio event histories, mistralai-workflows payload envelopes).
Packaged as mwf_conformance.capture; also runnable as plain scripts from a
repo checkout (see run_all.sh / the conformance Makefile)."""
