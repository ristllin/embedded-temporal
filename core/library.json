{
  "name": "core",
  "version": "0.1.0",
  "description": "mwf portable core: worker loop, client ops, replay engine, activity registry, retry. Portable C++17. NOTE: the .cpp are host-built (nlohmann/json) + host-tested in this repo; on device the JSON-lib-free portable TUs (worker_loop.cpp, activity_registry.cpp) compile too; the rest stays host-only.",
  "keywords": "mwf, temporal, replay, worker, esp32",
  "authors": [
    {
      "name": "Roy Darnell"
    }
  ],
  "license": "MIT",
  "frameworks": "*",
  "platforms": "*",
  "dependencies": {
    "contracts": "symlink://../contracts"
  },
  "build": {
    "srcFilter": [
      "-<*>",
      "+<worker_loop.cpp>",
      "+<activity_registry.cpp>",
      "+<workflow_loop.cpp>",
      "+<workflow_spec.cpp>",
      "+<replay_engine.cpp>",
      "+<history.cpp>",
      "+<json_util.cpp>",
      "+<client_ops.cpp>"
    ]
  }
}
